%center
  %h2= @alumno.nombre
= form_for @alumno, :url => alumno_path(@alumno), :html => { :class => "edit_alumno", :id => "edit_alumno" } do |f|
  %input{:name => "_method", :type => "hidden", :value =>"put"}
  = render :partial => "form", :locals => {:f => f}
